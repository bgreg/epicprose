<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @chapter.body %>
</p>

<p>
  <strong>Story:</strong>
  <%= @chapter.story_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @chapter.user_id %>
</p>

<%= link_to 'Edit', "/stories/#{params[:story_id]}/chapters/#{params[:id]}/edit" %> |
<%= link_to 'Back', "/stories/#{params[:story_id]}/chapters/#{params[:id]}" %>
